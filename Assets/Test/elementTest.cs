using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class elementTest : MonoBehaviour
{
    public float radius;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetMouseButtonDown(0))
        //{
        //    Vector2 targetPoint = Camera.main.ScreenToWorldPoint(Input.mousePosition);
        //    List<BasicElementSprite> elements =  ElementSpriteManager.instance.GetTypeElementsInCircle(targetPoint, radius, MagicType.Fire);
        //    foreach(var element in elements)
        //    {
        //        element.gameObject.SetActive(true);
        //        StartCoroutine(element.MoveToPos(targetPoint));
        //    }
        //}
    }
}
